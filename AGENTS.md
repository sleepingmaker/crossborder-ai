# AGENTS.md

本文件定义 CrossBorder AI 项目中 Cursor、Codex 等 AI 编程工具的协作规则。

## 1. 当前目标

当前阶段是学习型 MVP 开发，不允许为了“看起来完成”而一次性生成大量代码。

优先级：
1. 理解参考项目；
2. 明确数据流；
3. 实现最小闭环；
4. 写测试与文档；
5. 再扩展功能。

## 2. AI 工作规则

### 可以做
- 扫描仓库并解释结构；
- 画调用链和数据流；
- 把大任务拆成小任务；
- 实现单个明确 Issue；
- 写测试；
- 做代码审查；
- 解释错误和修复方案；
- 补充 README / PRD / 架构文档。

### 不可以直接做
- 一次性完成整个产品；
- 未解释设计就批量生成大量文件；
- 擅自引入复杂框架；
- 为了“Agent 化”而堆多 Agent；
- 未经确认改变 MVP 范围；
- 将密钥、Cookie、Token 写入仓库。

## 3. 每次开发任务必须先回答

在写代码前，AI 需要明确：
1. 这个任务解决什么问题？
2. 涉及哪些文件？
3. 输入是什么？
4. 输出是什么？
5. 如何验证完成？

## 4. 每次完成任务后必须输出

1. 修改了什么；
2. 为什么这样改；
3. 如何运行/验证；
4. 当前仍有哪些限制；
5. 项目负责人需要理解的关键代码位置。

## 5. 学习要求

项目负责人必须能用自己的话解释：
- 用户为什么需要这个功能；
- 一次请求的数据流；
- 核心模块职责；
- 关键技术选择；
- 当前方案的缺点；
- 如果需求变化应该改哪里。

如果这些问题无法回答，则该功能仍处于“AI 代写”状态，不视为完成学习。

## 6. 技术选择原则

- 优先简单、可运行、可解释；
- MVP 阶段优先单体应用；
- 能用普通函数解决时，不引入复杂 Agent 框架；
- 能用上传 CSV/JSON 验证价值时，不先做复杂爬虫；
- 外部服务必须通过环境变量配置；
- 必须提供 `.env.example`，禁止提交真实 `.env`。

## 7. Git 工作原则

- 一个 Issue 对应一个清晰任务；
- 小步提交；
- Commit 信息说明意图；
- 复杂功能先写设计/验收标准再编码；
- 主分支保持可运行。

## 8. 当前 MVP 范围

以 `docs/PRD.md` 为准。

若开发任务与 PRD 冲突，先停止编码并说明冲突，不要自行扩大范围。